{
  "name": "flashare",
  "version": "0.0.1",
  "description": "",
  "main": "./index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild ./src/lambdas/createSecret.ts --bundle --platform=node --outfile=./dist/lambdas/createSecret.js --target=node14",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "lgithub:lyng-dev/flashare"
  },
  "author": "",
  "license": "SEE LICENSE IN ./LICENSE",
  "dependencies": {
    "@types/aws-lambda": "^8.10.80",
    "@types/aws-sdk": "^2.7.0",
    "aws-lambda": "^1.0.6",
    "aws-sdk": "^2.953.0",
    "env-var": "^7.0.1",
    "tslint": "^6.1.3",
    "typescript": "^4.3.5"
  },
  "files": [
    "./bin/*",
    "./lib/*"
  ],
  "typings": "./lib/index.d.ts",
  "devDependencies": {
    "@types/node": "^16.4.1",
    "esbuild": "^0.12.15"
  }
}
